#include "mbed.h"

unsigned char a=3;

int main() {

float b=265.35;

    printf("a value is %d\n", a);
    printf("b value is %f\n", b);
    
    while (1) {
    wait_ms(500);
    }
}
